[project]
name = "image-recognition"
version = "0.1.0"
description = "Animal image classifier — AI Image Understanding capstone starter"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.0",
    "torchvision>=0.15",
    "numpy>=1.24,<2",
    "pillow",
    "matplotlib",
    "tqdm",
    "pydantic>=2.0",
    "python-dotenv",
    "openai>=1.0",
    "chromadb",
    "onnxruntime",
]
